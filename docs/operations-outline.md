# 运维文档实现提纲

Codex 在 Task 21 中把本提纲替换为可执行的 `docs/operations.md`。

必须覆盖：

- 环境变量；
- 镜像版本固定；
- MySQL/MinIO Volume；
- Alembic；
- Profile 首次登录；
- Profile 验证；
- API Key；
- 创建任务；
- 查询任务；
- 强制取消；
- 手动续跑；
- MinIO 清理；
- 数据库备份恢复；
- MinIO 备份恢复；
- Worker 异常和孤儿 Chromium 排查；
- Profile 失效恢复；
- 安全检查。
